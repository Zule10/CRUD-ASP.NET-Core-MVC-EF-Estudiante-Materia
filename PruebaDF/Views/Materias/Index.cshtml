@model IEnumerable<PruebaDF.Models.Materia>

@{
    ViewData["Title"] = "Index";
}

<h1>Materia</h1>

<div class="row justify-content-end">
    <div class="col-md-4">
        <a class="btn btn-primary" role="button" asp-action="Create">Crear</a>
    </div>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Codigo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Creditos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Codigo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Creditos)
            </td>
            <td>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.MateriaId">Modificar</a> 
                <a class="btn btn-secondary" asp-action="Details" asp-route-id="@item.MateriaId">Ver Detalle</a> 
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.MateriaId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
